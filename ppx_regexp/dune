(library
  (name ppx_regexp)
  (public_name ppx_regexp_extended)
  (kind ppx_rewriter)
  (modules util transformations ppx_regexp)
  (preprocess (pps ppxlib.metaquot))
  (libraries ppx_regexp_lib ppxlib re re.perl)
  (ppx_runtime_libraries re re.perl))
